this program demonstrate use of both dll and static library


there are two executables 

	* use_mingw_a.exe : one with static library *.a will run on alone

	* use_mingw_dll.exe : one with dll will always require the dll